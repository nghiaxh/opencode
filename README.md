# Opencode config

Global opencode configuration for commands and settings.

## Install

```bash
cd ~/.config/opencode
git init
git remote add origin https://github.com/nghiaxh/opencode.git
git fetch origin
git reset --hard origin/main
```

## Update

Edit config directly at `~/.config/opencode/`, then commit and push:

```powershell
cd $HOME\.config\opencode
git add -A
git commit -m "chore: update config"
git push
```

## Structure

```
opencode.jsonc              # Server and project config
cli.json                    # Terminal client preferences, owned by the TUI
AGENTS.md                   # Always-on instructions, the only file auto-loaded
commands/lazy-commit.md     # /lazy-commit
commands/lazy-audit.md      # /lazy-audit
commands/lazy-review.md     # /lazy-review
commands/lazy-debt.md       # /lazy-debt
service.json                # Generated by the v2 service, gitignored
```

## Skills

Not tracked here. They live in `~/.agents/skills/`, which OpenCode discovers
automatically, so a fresh clone starts with none installed.

```powershell
npx skills add vercel-labs/agent-skills -s vercel-react-best-practices -g -y
npx skills add vercel-labs/agent-skills -s web-design-guidelines -g -y
npx skills add vercel-labs/agent-skills -s vercel-composition-patterns -g -y
npx skills add wshobson/agents -s nextjs-app-router-patterns -g -y
npx skills add wshobson/agents -s react-state-management -g -y
npx skills add wshobson/agents -s accessibility-compliance -g -y
npx skills add wshobson/agents -s typescript-advanced-types -g -y
npx skills add github/awesome-copilot -s javascript-typescript-jest -g -y
npx skills add github/awesome-copilot -s java-springboot -g -y
npx skills add github/awesome-copilot -s spring-boot-testing -g -y
npx skills add github/awesome-copilot -s create-spring-boot-java-project -g -y
npx skills add github/awesome-copilot -s javax-to-jakarta-migration -g -y
npx skills add github/awesome-copilot -s sql-optimization -g -y
npx skills add Leonxlnx/taste-skill -s design-taste-frontend -g -y
npx skills add Leonxlnx/taste-skill -s minimalist-ui -g -y
```
